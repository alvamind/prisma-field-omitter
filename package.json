{
  "name": "prisma-field-omitter",
  "version": "1.0.0",
  "description": "A utility to omit fields from Prisma queries",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "prisma-field-omitter": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --minify && bun build ./src/cli.ts --outdir ./dist --target node --minify && chmod +x ./dist/cli.js",
    "prepublishOnly": "bun run clean && bun run build",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "prisma",
    "utilities",
    "field-omitter"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/prisma-field-omitter.git"
  },
  "dependencies": {
    "alvamind": "^1.0.10",
    "alvamind-tools": "^1.0.23",
    "ts-morph": "^25.0.1",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@prisma/client": "^5.0.0",
    "typescript": "^5.0.0"
  }
}